import type { NextPage } from "next";
import { EpisodesCardContainer } from "../../components/EpisodesCardContainer";

const EpisodesIndexPage: NextPage = () => {
  return (
    <div>
      <EpisodesCardContainer />
    </div>
  );
};

export default EpisodesIndexPage;
